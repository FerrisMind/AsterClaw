name: Docs Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install mdBook
        run: cargo install mdbook --locked

      - name: Build English (default)
        run: mdbook build docs

      - name: Build Russian
        run: mdbook build docs --config docs/book.ru.toml

      - name: Build Portuguese
        run: mdbook build docs --config docs/book.pt-br.toml

      - name: Assemble site
        run: |
          mkdir -p _site
          cp docs/index.html _site/
          cp -r docs/book/ru _site/ru
          cp -r docs/book/en _site/en
          cp -r docs/book/pt-br _site/pt-br

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
