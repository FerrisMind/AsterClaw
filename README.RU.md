<p align="left">
  <a href="README.md"><img src="https://img.shields.io/badge/English-232323" alt="English"></a>
  <a href="README.RU.md"><img src="https://img.shields.io/badge/Русский-D65C5C" alt="Русский"></a>
  <a href="README.PT_BR.md"><img src="https://img.shields.io/badge/Português_BR-232323" alt="Português"></a>
</p>

<h1 align="center">AsterClaw</h1>

<p align="center">
  Rust 2024 MVP-порт PicoClaw с работающим `gateway`, Telegram-каналом, OpenAI-совместимыми провайдерами и реальной командой `migrate`.
</p>

## 🎬 Демо

https://github.com/FerrisMind/AsterClaw/raw/main/res/demo.mp4

## 📚 Содержание

- [Что такое AsterClaw?](#-что-такое-asterclaw)
- [MVP](#-mvp)
- [Быстрые команды](#-быстрые-команды)
- [Конфигурация](#-конфигурация)
- [Стратегия провайдеров](#-стратегия-провайдеров)
- [Инструменты и сообщения](#-инструменты-и-сообщения)
- [Health, Cron, Heartbeat](#-health-cron-heartbeat)
- [Команда миграции](#-команда-миграции)
- [Запуск и тестирование](#-запуск-и-тестирование)
- [Как поучаствовать](#-как-поучаствовать)
- [Лицензия](#-лицензия)

## ✨ Что такое AsterClaw?

`AsterClaw` — CLI-шлюз на Rust 2024, воспроизводящий стек PicoClaw: агентный цикл, message bus, dispatch каналов. MVP фокусируется на:

- gateway + outbound notify
- Telegram-поллинг с allowlist / username-фильтром
- OpenAI-совместимая прослойка провайдеров (OpenAI/OpenRouter/Groq/Zhipu/DeepSeek)
- двойной конфиг (`.asterclaw` → `.picoclaw`), а также рабочая команда `migrate`
- cron-хранилище и health-пути `/health`, `/ready`

Цель — минимальный, но практичный MVP с `gateway`, Telegram, провайдерами и CLI (`migrate`, `cron`, `status`, `agent` и др.).

Каноничный контракт MVP: `mvp.md`.

## 🚀 MVP

| Зона | Описание |
| --- | --- |
| Gateway + цикл агентов | Прием входящих сообщений, вызов инструментов/провайдеров, публикация исходящих событий, уведомление каналов |
| Канал Telegram | Polling, allowlist/username, `/help`/`/list`/`/show`, безопасный markdown | 
| Провайдеры | OpenAI-совместимый слой с конфиг-управлением (провайдер → префикс модели → OpenRouter) |
| Инструменты | Файловые операции, защищенный `exec`, `web_search`, `web_fetch`, `message` |
| Конфиг + состояние | Двойное чтение (`.asterclaw` → `.picoclaw`), безопасные Windows-имена сессий, атомарные сохранения |
| Health & cron | `/health` + `/ready` + cron CRUD с JSON-персистом |
| Миграция | `asterclaw migrate` переносит legacy `.picoclaw` (конфиг + workspace) с dry-run, флагами и бэкапами |

## ⚡ Быстрые команды

- `cargo check`
- `cargo clippy -- -D warnings`
- `cargo test`
- `asterclaw gateway`
- `asterclaw cron list`
- `asterclaw migrate --dry-run`
- `asterclaw status`
- `pwsh scripts/nfr-harness.ps1`

Дополнительные команды (`agent`, `onboard`, `skills`, `auth`, `heartbeat`, `devices`) описаны в `PLAN.md`.

## 🛠️ Конфигурация

- **Основной конфиг:** `~/.asterclaw/config.json` (пишут onboarding, CLI-команды, cron).
- **Наследие:** если `.asterclaw` отсутствует, читаем `~/.picoclaw/config.json`, при необходимости конвертируя camelCase ↔ snake_case.
- **Workspace:** сессии, cron и навыки живут в `workspace.path` (или `workspace` в текущей папке) с атомарными сохранениями.
- **Состояние:** имена сессий очищаются (`:` → `_`) для Windows и сохраняются атоморно.

## 🎛️ Стратегия провайдеров

1. **OpenAI** — по умолчанию при `providers.openai` или префиксе `gpt-*`.
2. **OpenRouter** — fallback, если `api_base` указывает на `openrouter.ai` и провайдер явно не установлен.
3. **Groq / Zhipu / DeepSeek** — через общий OpenAI-совместимый интерфейс (выбор по конфигу / префиксу модели).
4. **Переменные окружения** — `OPENAI_API_KEY` / `OPENAI_API_BASE` и аналоги проверяются, но конфиг имеет приоритет.

Все провайдеры используют единую обработку ответов и tool call’ов, что упрощает тесты и согласованность.

## 🧱 Инструменты и сообщения

- Файловые инструменты защищены от directory traversal и не выполняют `canonicalize()` при записи новых файлов.
- `exec` применяет workspace policy и фильтрует опасные шаблоны перед запуском процессов.
- `web_search` вызывает Brave/DDG при включенном `web_search.enabled`.
- `web_fetch` скачивает URL и возвращает структурированный ответ для LLM.
- Инструмент `message` позволяет отправлять сообщения напрямую в канал без публикации outbound-события.

## 🧪 Health, Cron, Heartbeat

- HTTP-сервер health предоставляет `/health` (liveness) и `/ready` (готовность gateway).
- Cron-хранилище — `workspace/cron/jobs.json`; CLI команды поддерживают `add/list/remove/enable/disable`.
- Heartbeat в MVP минимален: периодический триггер включается только при явной настройке.

## 🔁 Команда миграции

`asterclaw migrate` — полноценная команда MVP:

1. `--dry-run` показывает план переноса без изменений.
2. `--config-only` / `--workspace-only` ограничивают объем.
3. `--force` делает бэкап и перезаписывает файлы в `~/.asterclaw/backups`.
4. Legacy provider keys, сессии и файлы workspace переносятся в новую структуру, а результат выводится (скопировано/пропущено/ошибки).

Миграция также очищает имена сессий для совместимости с Windows.

## 🧰 Запуск и тестирование

1. Установите Rust 2024 toolchain (`rust-toolchain.toml`) через `rustup`.
2. Запускайте `cargo check`, `cargo clippy -- -D warnings`, `cargo test`.
3. Локально запускайте `asterclaw gateway`; логи содержат agent loop, провайдеры и Telegram-поллинг.
4. В процессе разработки проверяйте `asterclaw cron list` и `asterclaw migrate --dry-run`.

## 🤝 Как поучаствовать

- Используйте план `PLAN.md`, чтобы понять текущие фазы.
- Синхронизируйте описание команд CLI с реальным поведением.
- Новые фичи документируйте на всех трех языках.
- При работе с конфигом/состоянием сохраняйте dual `.asterclaw`/`.picoclaw` совместимость.

Блокеры описаны в `error.md`.

## 📄 Лицензия

MIT — см. [LICENSE](LICENSE).
